// Unused: the app now always resolves the API base URL per-company from
// LoginContext.companyUrl (see ../../Config/Config.js -> useApiBaseUrl),
// set from the login response. No hardcoded URL is used anywhere.